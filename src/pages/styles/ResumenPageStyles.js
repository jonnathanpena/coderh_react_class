import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ResumenMainContainer = styled( 'div' )(() => ({
	padding: '1%',
}));

export const TitleResumenStyles = styled( Typography )(() => ({
  color: 'black',
	fontSize: '14px',
}));